// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.util.Comparator;
// imports+ 

// imports- 


/**
 * Comparator class is used to sort the MarketcodeBean objects.
 */
public class MarketcodeComparator implements Comparator
// extends+ 

// extends- 

{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for MarketcodeComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new MarketcodeComparator(MarketcodeManager.MARKETCODEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>MarketcodeManager.ID_MARKETCODEID
     *   <li>MarketcodeManager.ID_NAME
     * </ul>
     */
    public MarketcodeComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for MarketcodeComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new MarketcodeComparator(MarketcodeManager.MARKETCODEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>MarketcodeManager.ID_MARKETCODEID
     *   <li>MarketcodeManager.ID_NAME
     *   <li>MarketcodeManager.ID_STATUS
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public MarketcodeComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        MarketcodeBean b1 = (MarketcodeBean)pObj1;
        MarketcodeBean b2 = (MarketcodeBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case MarketcodeManager.ID_MARKETCODEID:
                if (b1.getMarketcodeid() == null && b2.getMarketcodeid() != null) {
                    iReturn = -1;
                } else if (b1.getMarketcodeid() == null && b2.getMarketcodeid() == null) {
                    iReturn = 0;
                } else if (b1.getMarketcodeid() != null && b2.getMarketcodeid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getMarketcodeid().compareTo(b2.getMarketcodeid());
                }
                break;
            case MarketcodeManager.ID_NAME:
                if (b1.getName() == null && b2.getName() != null) {
                    iReturn = -1;
                } else if (b1.getName() == null && b2.getName() == null) {
                    iReturn = 0;
                } else if (b1.getName() != null && b2.getName() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getName().compareTo(b2.getName());
                }
                break;
            default: 
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }
// class+ 

// class- 

}
