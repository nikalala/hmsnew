// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.sql.*;
// imports+ 

// imports- 

/**
 * Handles database calls for the menu table.
 */
public class MenuManager
// extends+ 

// extends- 
{

    /**
     * Column menuid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_MENUID = 0;
    public static final int TYPE_MENUID = Types.INTEGER;
    public static final String NAME_MENUID = "menuid";

    /**
     * Column name of type Types.VARCHAR mapped to String.
     */
    public static final int ID_NAME = 1;
    public static final int TYPE_NAME = Types.VARCHAR;
    public static final String NAME_NAME = "name";

    /**
     * Column file of type Types.VARCHAR mapped to String.
     */
    public static final int ID_FILE = 2;
    public static final int TYPE_FILE = Types.VARCHAR;
    public static final String NAME_FILE = "file";

    /**
     * Column parentid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_PARENTID = 3;
    public static final int TYPE_PARENTID = Types.INTEGER;
    public static final String NAME_PARENTID = "parentid";

    /**
     * Column ord of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_ORD = 4;
    public static final int TYPE_ORD = Types.INTEGER;
    public static final String NAME_ORD = "ord";


    private static final String TABLE_NAME = "menu";

    /**
     * Create an array of type string containing all the fields of the menu table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "menu.menuid"
        ,"menu.name"
        ,"menu.file"
        ,"menu.parentid"
        ,"menu.ord"
    };

    /**
     * Field that contains the comma separated fields of the menu table.
     */
    private static final String ALL_FIELDS = "menu.menuid"
                            + ",menu.name"
                            + ",menu.file"
                            + ",menu.parentid"
                            + ",menu.ord";

    private static MenuManager singleton = new MenuManager();

    /**
     * Get the MenuManager singleton.
     *
     * @return MenuManager 
     */
    synchronized public static MenuManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own MenuManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(MenuManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new MenuBean instance.
     *
     * @return the new MenuBean 
     */
    public MenuBean createMenuBean()
    {
        return new MenuBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a MenuBean from the menu using its key fields.
     *
     * @return a unique MenuBean 
     */
    //12
    public MenuBean loadByPrimaryKey(Integer menuid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM menu WHERE menu.menuid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, menuid);
            MenuBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Integer menuid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from menu WHERE menu.menuid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, menuid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads MenuBean array from the menu table using its parentid field.
     *
     * @return an array of MenuBean 
     */
    // LOAD BY IMPORTED KEY
    public MenuBean[] loadByParentid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM menu WHERE parentid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the menu table by parentid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByParentid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM menu WHERE parentid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the MenuBean object from the menu.menuid field.
     *
     * @param pObject the MenuBean 
     * @return the associated MenuBean pObject
     */
    // GET IMPORTED
    public MenuBean getMenuBean(MenuBean pObject) throws SQLException
    {
        MenuBean other = MenuManager.getInstance().createMenuBean();
        other.setMenuid(pObject.getParentid());
        return MenuManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the MenuBean object to the MenuBean object.
     *
     * @param pObject the MenuBean object to use
     * @param pObjectToBeSet the MenuBean object to associate to the MenuBean 
     * @return the associated MenuBean pObject
     */
    // SET IMPORTED
    public MenuBean setMenuBean(MenuBean pObject,MenuBean pObjectToBeSet)
    {
        pObject.setParentid(pObjectToBeSet.getMenuid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from menu.
     *
     * @return an array of MenuManager pObject
     */
    //38
    public MenuBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM menu",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of MenuBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting MenuBean table 
     */
    //49
    public MenuBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of MenuBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting MenuBean table 
     */
    //51
    public MenuBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from menu " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from menu ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (MenuBean[])v.toArray(new MenuBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from menu table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the menu table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM menu " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the MenuBean pObject into the database.
     *
     * @param pObject the MenuBean pObject to be saved
     */
    //100
    public MenuBean save(MenuBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                if (!pObject.isMenuidModified())
                {
                    ps = c.prepareStatement("SELECT nextval('menuid_seq')");
                    ResultSet rs = null;
                    try
                    {
                        rs = ps.executeQuery();
                        if(rs.next())
                            pObject.setMenuid(Manager.getInteger(rs, 1));
                        else
                            getManager().log("ATTENTION: Could not retrieve generated key!");
                    }
                    finally
                    {
                        getManager().close(ps, rs);
                        ps=null;
                    }
                }
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into menu (");
    
                if (pObject.isMenuidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("menuid");
                    _dirtyCount++;
                }

                if (pObject.isNameModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("name");
                    _dirtyCount++;
                }

                if (pObject.isFileModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("file");
                    _dirtyCount++;
                }

                if (pObject.isParentidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("parentid");
                    _dirtyCount++;
                }

                if (pObject.isOrdModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("ord");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isMenuidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }
    
                if (pObject.isNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getName());
                }
    
                if (pObject.isFileModified()) {
                    ps.setString(++_dirtyCount, pObject.getFile());
                }
    
                if (pObject.isParentidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getParentid());
                }
    
                if (pObject.isOrdModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getOrd());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE menu SET ");
                boolean useComma=false;

                if (pObject.isMenuidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("menuid").append("=?");
                }

                if (pObject.isNameModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("name").append("=?");
                }

                if (pObject.isFileModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("file").append("=?");
                }

                if (pObject.isParentidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("parentid").append("=?");
                }

                if (pObject.isOrdModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("ord").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("menu.menuid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isMenuidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }

                if (pObject.isNameModified()) {
                      ps.setString(++_dirtyCount, pObject.getName());
                }

                if (pObject.isFileModified()) {
                      ps.setString(++_dirtyCount, pObject.getFile());
                }

                if (pObject.isParentidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getParentid());
                }

                if (pObject.isOrdModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getOrd());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of MenuBean pObjects into the database.
     *
     * @param pObjects the MenuBean pObject table to be saved
     * @return the saved MenuBean array.
     */
    //65
    public MenuBean[] save(MenuBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique MenuBean pObject from a template one giving a c
     *
     * @param pObject the MenuBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public MenuBean loadUniqueUsingTemplate(MenuBean pObject) throws SQLException
    {
         MenuBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of MenuBean from a template one.
     *
     * @param pObject the MenuBean template to look for
     * @return all the MenuBean matching the template
     */
    //88
    public MenuBean[] loadUsingTemplate(MenuBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from menu WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isMenuidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("menuid= ?");
             }
    
             if (pObject.isNameModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("name= ?");
             }
    
             if (pObject.isFileModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("file= ?");
             }
    
             if (pObject.isParentidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("parentid= ?");
             }
    
             if (pObject.isOrdModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("ord= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isMenuidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
             }
    
             if (pObject.isNameModified()) {
                 ps.setString(++_dirtyCount, pObject.getName());
             }
    
             if (pObject.isFileModified()) {
                 ps.setString(++_dirtyCount, pObject.getFile());
             }
    
             if (pObject.isParentidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getParentid());
             }
    
             if (pObject.isOrdModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getOrd());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a MenuBean template.
     *
     * @param pObject the MenuBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(MenuBean pObject) throws SQLException
    {
        if (pObject.isMenuidInitialized())
            return deleteByPrimaryKey(pObject.getMenuid());
    
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM menu WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isMenuidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("menuid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isNameInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("name").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isFileInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("file").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isParentidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("parentid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isOrdInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("ord").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isMenuidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
            }
    
            if (pObject.isNameInitialized()) {
                ps.setString(++_dirtyCount, pObject.getName());
            }
    
            if (pObject.isFileInitialized()) {
                ps.setString(++_dirtyCount, pObject.getFile());
            }
    
            if (pObject.isParentidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getParentid());
            }
    
            if (pObject.isOrdInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getOrd());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    ///////////////////////////////////////////////////////////////////////
    // MANY TO MANY: LOAD OTHER BEAN VIA JUNCTION TABLE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Retrieves an array of PersonneltypeBean using the relation table Plevel given a MenuBean object.
     *
     * @param pObject the MenuBean pObject to be used
     * @return an array of PersonneltypeBean 
     */
    // MANY TO MANY
    public PersonneltypeBean[] loadPersonneltypeViaPlevel(MenuBean pObject) throws SQLException
    {
         Connection c = null;
         PreparedStatement ps = null;
         String strSQL =      " SELECT "
                         + "        *"
                         + " FROM  "
                         + "        personneltype,plevel"
                         + " WHERE "    
                         + "     plevel.menuid = ?"
                         + " AND plevel.personneltypeid = personneltype.personneltypeid";
         try
         {
             c = getConnection();
             ps = c.prepareStatement(strSQL,ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             Manager.setInteger(ps, 1, pObject.getMenuid());
             return PersonneltypeManager.getInstance().loadByPreparedStatement(ps);
         }
         finally
         {
            getManager().close(ps);
            freeConnection(c);
         }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table menu.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table menu with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from menu " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table menu with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific MenuBean pObject given a c
     *
     * @param pObject the MenuBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(MenuBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from menu WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isMenuidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("menuid= ?");
                }
    
                if (pObject.isNameModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("name= ?");
                }
    
                if (pObject.isFileModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("file= ?");
                }
    
                if (pObject.isParentidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("parentid= ?");
                }
    
                if (pObject.isOrdModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("ord= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isMenuidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }
    
                if (pObject.isNameModified()) {
                    ps.setString(++_dirtyCount, pObject.getName());
                }
    
                if (pObject.isFileModified()) {
                    ps.setString(++_dirtyCount, pObject.getFile());
                }
    
                if (pObject.isParentidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getParentid());
                }
    
                if (pObject.isOrdModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getOrd());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the menu on a MenuBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting MenuBean pObject
     */
    //72
    public MenuBean decodeRow(ResultSet rs) throws SQLException
    {
        MenuBean pObject = createMenuBean();
        pObject.setMenuid(Manager.getInteger(rs, 1));
        pObject.setName(rs.getString(2));
        pObject.setFile(rs.getString(3));
        pObject.setParentid(Manager.getInteger(rs, 4));
        pObject.setOrd(Manager.getInteger(rs, 5));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the menu table on a MenuBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting MenuBean pObject
     */
    //73
    public MenuBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        MenuBean pObject = createMenuBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_MENUID:
                    ++pos;
                    pObject.setMenuid(Manager.getInteger(rs, pos));
                    break;
                case ID_NAME:
                    ++pos;
                    pObject.setName(rs.getString(pos));
                    break;
                case ID_FILE:
                    ++pos;
                    pObject.setFile(rs.getString(pos));
                    break;
                case ID_PARENTID:
                    ++pos;
                    pObject.setParentid(Manager.getInteger(rs, pos));
                    break;
                case ID_ORD:
                    ++pos;
                    pObject.setOrd(Manager.getInteger(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of MenuBean 
     */
    //41
    public MenuBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of MenuBean 
     */
    public MenuBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (MenuBean[])v.toArray(new MenuBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private MenuListener listener = null;

    /**
     * Registers a unique MenuListener listener.
     */
    //66.5
    public void registerListener(MenuListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the MenuBean pObject.
     *
     * @param pObject the MenuBean pObject to be saved
     */
    //67
    void beforeInsert(MenuBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the MenuBean pObject.
     *
     * @param pObject the MenuBean pObject to be saved
     */
    //68
    void afterInsert(MenuBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the MenuBean pObject.
     *
     * @param pObject the MenuBean pObject to be updated
     */
    //69
    void beforeUpdate(MenuBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the MenuBean pObject.
     *
     * @param pObject the MenuBean pObject to be updated
     */
    //70
    void afterUpdate(MenuBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 
}
