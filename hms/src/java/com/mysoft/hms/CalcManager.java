// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.sql.*;
// imports+ 

// imports- 


/**
 * Handles database calls for the calc table.
 */
public class CalcManager
// extends+ 

// extends- 

{

    /**
     * Column sproductpriceid of type Types.BIGINT mapped to Long.
     */
    public static final int ID_SPRODUCTPRICEID = 0;
    public static final int TYPE_SPRODUCTPRICEID = Types.BIGINT;
    public static final String NAME_SPRODUCTPRICEID = "sproductpriceid";

    /**
     * Column productid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_PRODUCTID = 1;
    public static final int TYPE_PRODUCTID = Types.INTEGER;
    public static final String NAME_PRODUCTID = "productid";

    /**
     * Column amount of type Types.DOUBLE mapped to Double.
     */
    public static final int ID_AMOUNT = 2;
    public static final int TYPE_AMOUNT = Types.DOUBLE;
    public static final String NAME_AMOUNT = "amount";


    private static final String TABLE_NAME = "calc";

    /**
     * Create an array of type string containing all the fields of the calc table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "calc.sproductpriceid"
        ,"calc.productid"
        ,"calc.amount"
    };

    /**
     * Field that contains the comma separated fields of the calc table.
     */
    private static final String ALL_FIELDS = "calc.sproductpriceid"
                            + ",calc.productid"
                            + ",calc.amount";

    private static CalcManager singleton = new CalcManager();

    /**
     * Get the CalcManager singleton.
     *
     * @return CalcManager 
     */
    synchronized public static CalcManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own CalcManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(CalcManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new CalcBean instance.
     *
     * @return the new CalcBean 
     */
    public CalcBean createCalcBean()
    {
        return new CalcBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a CalcBean from the calc using its key fields.
     *
     * @return a unique CalcBean 
     */
    //12
    public CalcBean loadByPrimaryKey(Long sproductpriceid, Integer productid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM calc WHERE calc.sproductpriceid=? and calc.productid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, sproductpriceid);
            Manager.setInteger(ps, 2, productid);
            CalcBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Long sproductpriceid, Integer productid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from calc WHERE calc.sproductpriceid=? and calc.productid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, sproductpriceid);
            Manager.setInteger(ps, 2, productid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads CalcBean array from the calc table using its productid field.
     *
     * @return an array of CalcBean 
     */
    // LOAD BY IMPORTED KEY
    public CalcBean[] loadByProductid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM calc WHERE productid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the calc table by productid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByProductid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM calc WHERE productid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Loads CalcBean array from the calc table using its sproductpriceid field.
     *
     * @return an array of CalcBean 
     */
    // LOAD BY IMPORTED KEY
    public CalcBean[] loadBySproductpriceid(Long value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM calc WHERE sproductpriceid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setLong(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the calc table by sproductpriceid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteBySproductpriceid(Long value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM calc WHERE sproductpriceid=?");
            Manager.setLong(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the ProductBean object from the calc.productid field.
     *
     * @param pObject the CalcBean 
     * @return the associated ProductBean pObject
     */
    // GET IMPORTED
    public ProductBean getProductBean(CalcBean pObject) throws SQLException
    {
        ProductBean other = ProductManager.getInstance().createProductBean();
        other.setProductid(pObject.getProductid());
        return ProductManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the CalcBean object to the ProductBean object.
     *
     * @param pObject the CalcBean object to use
     * @param pObjectToBeSet the ProductBean object to associate to the CalcBean 
     * @return the associated ProductBean pObject
     */
    // SET IMPORTED
    public CalcBean setProductBean(CalcBean pObject,ProductBean pObjectToBeSet)
    {
        pObject.setProductid(pObjectToBeSet.getProductid());
        return pObject;
    }

    /**
     * Retrieves the SproductpriceBean object from the calc.sproductpriceid field.
     *
     * @param pObject the CalcBean 
     * @return the associated SproductpriceBean pObject
     */
    // GET IMPORTED
    public SproductpriceBean getSproductpriceBean(CalcBean pObject) throws SQLException
    {
        SproductpriceBean other = SproductpriceManager.getInstance().createSproductpriceBean();
        other.setSproductpriceid(pObject.getSproductpriceid());
        return SproductpriceManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the CalcBean object to the SproductpriceBean object.
     *
     * @param pObject the CalcBean object to use
     * @param pObjectToBeSet the SproductpriceBean object to associate to the CalcBean 
     * @return the associated SproductpriceBean pObject
     */
    // SET IMPORTED
    public CalcBean setSproductpriceBean(CalcBean pObject,SproductpriceBean pObjectToBeSet)
    {
        pObject.setSproductpriceid(pObjectToBeSet.getSproductpriceid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from calc.
     *
     * @return an array of CalcManager pObject
     */
    //38
    public CalcBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM calc",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of CalcBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting CalcBean table 
     */
    //49
    public CalcBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of CalcBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting CalcBean table 
     */
    //51
    public CalcBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from calc " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from calc ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (CalcBean[])v.toArray(new CalcBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from calc table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the calc table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM calc " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the CalcBean pObject into the database.
     *
     * @param pObject the CalcBean pObject to be saved
     */
    //100
    public CalcBean save(CalcBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into calc (");
    
                if (pObject.isSproductpriceidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("sproductpriceid");
                    _dirtyCount++;
                }

                if (pObject.isProductidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("productid");
                    _dirtyCount++;
                }

                if (pObject.isAmountModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("amount");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isSproductpriceidModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
                }
    
                if (pObject.isProductidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
                }
    
                if (pObject.isAmountModified()) {
                    Manager.setDouble(ps, ++_dirtyCount, pObject.getAmount());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE calc SET ");
                boolean useComma=false;

                if (pObject.isSproductpriceidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("sproductpriceid").append("=?");
                }

                if (pObject.isProductidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("productid").append("=?");
                }

                if (pObject.isAmountModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("amount").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("calc.sproductpriceid=? AND calc.productid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isSproductpriceidModified()) {
                      Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
                }

                if (pObject.isProductidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
                }

                if (pObject.isAmountModified()) {
                      Manager.setDouble(ps, ++_dirtyCount, pObject.getAmount());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
                Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of CalcBean pObjects into the database.
     *
     * @param pObjects the CalcBean pObject table to be saved
     * @return the saved CalcBean array.
     */
    //65
    public CalcBean[] save(CalcBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique CalcBean pObject from a template one giving a c
     *
     * @param pObject the CalcBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public CalcBean loadUniqueUsingTemplate(CalcBean pObject) throws SQLException
    {
         CalcBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of CalcBean from a template one.
     *
     * @param pObject the CalcBean template to look for
     * @return all the CalcBean matching the template
     */
    //88
    public CalcBean[] loadUsingTemplate(CalcBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from calc WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isSproductpriceidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sproductpriceid= ?");
             }
    
             if (pObject.isProductidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("productid= ?");
             }
    
             if (pObject.isAmountModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("amount= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isSproductpriceidModified()) {
                 Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
             }
    
             if (pObject.isProductidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
             }
    
             if (pObject.isAmountModified()) {
                 Manager.setDouble(ps, ++_dirtyCount, pObject.getAmount());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a CalcBean template.
     *
     * @param pObject the CalcBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(CalcBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM calc WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isSproductpriceidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("sproductpriceid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isProductidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("productid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isAmountInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("amount").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isSproductpriceidInitialized()) {
                Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
            }
    
            if (pObject.isProductidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
            }
    
            if (pObject.isAmountInitialized()) {
                Manager.setDouble(ps, ++_dirtyCount, pObject.getAmount());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table calc.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table calc with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from calc " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table calc with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific CalcBean pObject given a c
     *
     * @param pObject the CalcBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(CalcBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from calc WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isSproductpriceidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("sproductpriceid= ?");
                }
    
                if (pObject.isProductidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("productid= ?");
                }
    
                if (pObject.isAmountModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("amount= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isSproductpriceidModified()) {
                    Manager.setLong(ps, ++_dirtyCount, pObject.getSproductpriceid());
                }
    
                if (pObject.isProductidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getProductid());
                }
    
                if (pObject.isAmountModified()) {
                    Manager.setDouble(ps, ++_dirtyCount, pObject.getAmount());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the calc on a CalcBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting CalcBean pObject
     */
    //72
    public CalcBean decodeRow(ResultSet rs) throws SQLException
    {
        CalcBean pObject = createCalcBean();
        pObject.setSproductpriceid(Manager.getLong(rs, 1));
        pObject.setProductid(Manager.getInteger(rs, 2));
        pObject.setAmount(Manager.getDouble(rs, 3));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the calc table on a CalcBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting CalcBean pObject
     */
    //73
    public CalcBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        CalcBean pObject = createCalcBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_SPRODUCTPRICEID:
                    ++pos;
                    pObject.setSproductpriceid(Manager.getLong(rs, pos));
                    break;
                case ID_PRODUCTID:
                    ++pos;
                    pObject.setProductid(Manager.getInteger(rs, pos));
                    break;
                case ID_AMOUNT:
                    ++pos;
                    pObject.setAmount(Manager.getDouble(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of CalcBean 
     */
    //41
    public CalcBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of CalcBean 
     */
    public CalcBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (CalcBean[])v.toArray(new CalcBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private CalcListener listener = null;

    /**
     * Registers a unique CalcListener listener.
     */
    //66.5
    public void registerListener(CalcListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the CalcBean pObject.
     *
     * @param pObject the CalcBean pObject to be saved
     */
    //67
    void beforeInsert(CalcBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the CalcBean pObject.
     *
     * @param pObject the CalcBean pObject to be saved
     */
    //68
    void afterInsert(CalcBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the CalcBean pObject.
     *
     * @param pObject the CalcBean pObject to be updated
     */
    //69
    void beforeUpdate(CalcBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the CalcBean pObject.
     *
     * @param pObject the CalcBean pObject to be updated
     */
    //70
    void afterUpdate(CalcBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 

}
