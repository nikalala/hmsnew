// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.util.Comparator;
// imports+ 

// imports- 

/**
 * Comparator class is used to sort the TaxBean objects.
 */
public class TaxComparator implements Comparator
// extends+ 

// extends- 
{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for TaxComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TaxComparator(TaxManager.TAXID, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TaxManager.ID_TAXID
     *   <li>TaxManager.ID_NAME
     *   <li>TaxManager.ID_CODE
     *   <li>TaxManager.ID_STARTFROM
     *   <li>TaxManager.ID_EXEMPTAFTER
     *   <li>TaxManager.ID_POSTINGTYPE
     *   <li>TaxManager.ID_AMOUNT
     *   <li>TaxManager.ID_REGBYID
     *   <li>TaxManager.ID_REGDATE
     * </ul>
     */
    public TaxComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for TaxComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new TaxComparator(TaxManager.TAXID, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>TaxManager.ID_TAXID
     *   <li>TaxManager.ID_NAME
     *   <li>TaxManager.ID_CODE
     *   <li>TaxManager.ID_STARTFROM
     *   <li>TaxManager.ID_EXEMPTAFTER
     *   <li>TaxManager.ID_POSTINGTYPE
     *   <li>TaxManager.ID_AMOUNT
     *   <li>TaxManager.ID_APPLYBEFORE
     *   <li>TaxManager.ID_APPLYONRATE
     *   <li>TaxManager.ID_REGBYID
     *   <li>TaxManager.ID_REGDATE
     *   <li>TaxManager.ID_ACTIVE
     *   <li>TaxManager.ID_DELETED
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public TaxComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        TaxBean b1 = (TaxBean)pObj1;
        TaxBean b2 = (TaxBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case TaxManager.ID_TAXID:
                if (b1.getTaxid() == null && b2.getTaxid() != null) {
                    iReturn = -1;
                } else if (b1.getTaxid() == null && b2.getTaxid() == null) {
                    iReturn = 0;
                } else if (b1.getTaxid() != null && b2.getTaxid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getTaxid().compareTo(b2.getTaxid());
                }
                break;
            case TaxManager.ID_NAME:
                if (b1.getName() == null && b2.getName() != null) {
                    iReturn = -1;
                } else if (b1.getName() == null && b2.getName() == null) {
                    iReturn = 0;
                } else if (b1.getName() != null && b2.getName() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getName().compareTo(b2.getName());
                }
                break;
            case TaxManager.ID_CODE:
                if (b1.getCode() == null && b2.getCode() != null) {
                    iReturn = -1;
                } else if (b1.getCode() == null && b2.getCode() == null) {
                    iReturn = 0;
                } else if (b1.getCode() != null && b2.getCode() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getCode().compareTo(b2.getCode());
                }
                break;
            case TaxManager.ID_STARTFROM:
                if (b1.getStartfrom() == null && b2.getStartfrom() != null) {
                    iReturn = -1;
                } else if (b1.getStartfrom() == null && b2.getStartfrom() == null) {
                    iReturn = 0;
                } else if (b1.getStartfrom() != null && b2.getStartfrom() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getStartfrom().compareTo(b2.getStartfrom());
                }
                break;
            case TaxManager.ID_EXEMPTAFTER:
                if (b1.getExemptafter() == null && b2.getExemptafter() != null) {
                    iReturn = -1;
                } else if (b1.getExemptafter() == null && b2.getExemptafter() == null) {
                    iReturn = 0;
                } else if (b1.getExemptafter() != null && b2.getExemptafter() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getExemptafter().compareTo(b2.getExemptafter());
                }
                break;
            case TaxManager.ID_POSTINGTYPE:
                if (b1.getPostingtype() == null && b2.getPostingtype() != null) {
                    iReturn = -1;
                } else if (b1.getPostingtype() == null && b2.getPostingtype() == null) {
                    iReturn = 0;
                } else if (b1.getPostingtype() != null && b2.getPostingtype() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getPostingtype().compareTo(b2.getPostingtype());
                }
                break;
            case TaxManager.ID_AMOUNT:
                if (b1.getAmount() == null && b2.getAmount() != null) {
                    iReturn = -1;
                } else if (b1.getAmount() == null && b2.getAmount() == null) {
                    iReturn = 0;
                } else if (b1.getAmount() != null && b2.getAmount() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getAmount().compareTo(b2.getAmount());
                }
                break;
            case TaxManager.ID_REGBYID:
                if (b1.getRegbyid() == null && b2.getRegbyid() != null) {
                    iReturn = -1;
                } else if (b1.getRegbyid() == null && b2.getRegbyid() == null) {
                    iReturn = 0;
                } else if (b1.getRegbyid() != null && b2.getRegbyid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getRegbyid().compareTo(b2.getRegbyid());
                }
                break;
            case TaxManager.ID_REGDATE:
                if (b1.getRegdate() == null && b2.getRegdate() != null) {
                    iReturn = -1;
                } else if (b1.getRegdate() == null && b2.getRegdate() == null) {
                    iReturn = 0;
                } else if (b1.getRegdate() != null && b2.getRegdate() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getRegdate().compareTo(b2.getRegdate());
                }
                break;
            default: 
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }
// class+ 

// class- 
}
