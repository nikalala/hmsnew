// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.util.Comparator;
// imports+ 

// imports- 


/**
 * Comparator class is used to sort the RoomtypeamenityBean objects.
 */
public class RoomtypeamenityComparator implements Comparator
// extends+ 

// extends- 

{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for RoomtypeamenityComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new RoomtypeamenityComparator(RoomtypeamenityManager.ROOMTYPEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>RoomtypeamenityManager.ID_ROOMTYPEID
     *   <li>RoomtypeamenityManager.ID_AMENITYID
     * </ul>
     */
    public RoomtypeamenityComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for RoomtypeamenityComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new RoomtypeamenityComparator(RoomtypeamenityManager.ROOMTYPEID, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>RoomtypeamenityManager.ID_ROOMTYPEID
     *   <li>RoomtypeamenityManager.ID_AMENITYID
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public RoomtypeamenityComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        RoomtypeamenityBean b1 = (RoomtypeamenityBean)pObj1;
        RoomtypeamenityBean b2 = (RoomtypeamenityBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case RoomtypeamenityManager.ID_ROOMTYPEID:
                if (b1.getRoomtypeid() == null && b2.getRoomtypeid() != null) {
                    iReturn = -1;
                } else if (b1.getRoomtypeid() == null && b2.getRoomtypeid() == null) {
                    iReturn = 0;
                } else if (b1.getRoomtypeid() != null && b2.getRoomtypeid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getRoomtypeid().compareTo(b2.getRoomtypeid());
                }
                break;
            case RoomtypeamenityManager.ID_AMENITYID:
                if (b1.getAmenityid() == null && b2.getAmenityid() != null) {
                    iReturn = -1;
                } else if (b1.getAmenityid() == null && b2.getAmenityid() == null) {
                    iReturn = 0;
                } else if (b1.getAmenityid() != null && b2.getAmenityid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getAmenityid().compareTo(b2.getAmenityid());
                }
                break;
            default: 
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }
// class+ 

// class- 

}
