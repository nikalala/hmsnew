// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.sql.*;
// imports+ 

// imports- 


/**
 * Handles database calls for the roomtypeamenity table.
 */
public class RoomtypeamenityManager
// extends+ 

// extends- 

{

    /**
     * Column roomtypeid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_ROOMTYPEID = 0;
    public static final int TYPE_ROOMTYPEID = Types.INTEGER;
    public static final String NAME_ROOMTYPEID = "roomtypeid";

    /**
     * Column amenityid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_AMENITYID = 1;
    public static final int TYPE_AMENITYID = Types.INTEGER;
    public static final String NAME_AMENITYID = "amenityid";


    private static final String TABLE_NAME = "roomtypeamenity";

    /**
     * Create an array of type string containing all the fields of the roomtypeamenity table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "roomtypeamenity.roomtypeid"
        ,"roomtypeamenity.amenityid"
    };

    /**
     * Field that contains the comma separated fields of the roomtypeamenity table.
     */
    private static final String ALL_FIELDS = "roomtypeamenity.roomtypeid"
                            + ",roomtypeamenity.amenityid";

    private static RoomtypeamenityManager singleton = new RoomtypeamenityManager();

    /**
     * Get the RoomtypeamenityManager singleton.
     *
     * @return RoomtypeamenityManager 
     */
    synchronized public static RoomtypeamenityManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own RoomtypeamenityManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(RoomtypeamenityManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new RoomtypeamenityBean instance.
     *
     * @return the new RoomtypeamenityBean 
     */
    public RoomtypeamenityBean createRoomtypeamenityBean()
    {
        return new RoomtypeamenityBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a RoomtypeamenityBean from the roomtypeamenity using its key fields.
     *
     * @return a unique RoomtypeamenityBean 
     */
    //12
    public RoomtypeamenityBean loadByPrimaryKey(Integer roomtypeid, Integer amenityid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM roomtypeamenity WHERE roomtypeamenity.roomtypeid=? and roomtypeamenity.amenityid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, roomtypeid);
            Manager.setInteger(ps, 2, amenityid);
            RoomtypeamenityBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Integer roomtypeid, Integer amenityid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from roomtypeamenity WHERE roomtypeamenity.roomtypeid=? and roomtypeamenity.amenityid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, roomtypeid);
            Manager.setInteger(ps, 2, amenityid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads RoomtypeamenityBean array from the roomtypeamenity table using its amenityid field.
     *
     * @return an array of RoomtypeamenityBean 
     */
    // LOAD BY IMPORTED KEY
    public RoomtypeamenityBean[] loadByAmenityid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM roomtypeamenity WHERE amenityid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the roomtypeamenity table by amenityid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByAmenityid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM roomtypeamenity WHERE amenityid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Loads RoomtypeamenityBean array from the roomtypeamenity table using its roomtypeid field.
     *
     * @return an array of RoomtypeamenityBean 
     */
    // LOAD BY IMPORTED KEY
    public RoomtypeamenityBean[] loadByRoomtypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM roomtypeamenity WHERE roomtypeid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the roomtypeamenity table by roomtypeid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByRoomtypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM roomtypeamenity WHERE roomtypeid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the AmenityBean object from the roomtypeamenity.amenityid field.
     *
     * @param pObject the RoomtypeamenityBean 
     * @return the associated AmenityBean pObject
     */
    // GET IMPORTED
    public AmenityBean getAmenityBean(RoomtypeamenityBean pObject) throws SQLException
    {
        AmenityBean other = AmenityManager.getInstance().createAmenityBean();
        other.setAmenityid(pObject.getAmenityid());
        return AmenityManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the RoomtypeamenityBean object to the AmenityBean object.
     *
     * @param pObject the RoomtypeamenityBean object to use
     * @param pObjectToBeSet the AmenityBean object to associate to the RoomtypeamenityBean 
     * @return the associated AmenityBean pObject
     */
    // SET IMPORTED
    public RoomtypeamenityBean setAmenityBean(RoomtypeamenityBean pObject,AmenityBean pObjectToBeSet)
    {
        pObject.setAmenityid(pObjectToBeSet.getAmenityid());
        return pObject;
    }

    /**
     * Retrieves the RoomtypeBean object from the roomtypeamenity.roomtypeid field.
     *
     * @param pObject the RoomtypeamenityBean 
     * @return the associated RoomtypeBean pObject
     */
    // GET IMPORTED
    public RoomtypeBean getRoomtypeBean(RoomtypeamenityBean pObject) throws SQLException
    {
        RoomtypeBean other = RoomtypeManager.getInstance().createRoomtypeBean();
        other.setRoomtypeid(pObject.getRoomtypeid());
        return RoomtypeManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the RoomtypeamenityBean object to the RoomtypeBean object.
     *
     * @param pObject the RoomtypeamenityBean object to use
     * @param pObjectToBeSet the RoomtypeBean object to associate to the RoomtypeamenityBean 
     * @return the associated RoomtypeBean pObject
     */
    // SET IMPORTED
    public RoomtypeamenityBean setRoomtypeBean(RoomtypeamenityBean pObject,RoomtypeBean pObjectToBeSet)
    {
        pObject.setRoomtypeid(pObjectToBeSet.getRoomtypeid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from roomtypeamenity.
     *
     * @return an array of RoomtypeamenityManager pObject
     */
    //38
    public RoomtypeamenityBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM roomtypeamenity",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of RoomtypeamenityBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting RoomtypeamenityBean table 
     */
    //49
    public RoomtypeamenityBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of RoomtypeamenityBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting RoomtypeamenityBean table 
     */
    //51
    public RoomtypeamenityBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from roomtypeamenity " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from roomtypeamenity ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (RoomtypeamenityBean[])v.toArray(new RoomtypeamenityBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from roomtypeamenity table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the roomtypeamenity table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM roomtypeamenity " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the RoomtypeamenityBean pObject into the database.
     *
     * @param pObject the RoomtypeamenityBean pObject to be saved
     */
    //100
    public RoomtypeamenityBean save(RoomtypeamenityBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into roomtypeamenity (");
    
                if (pObject.isRoomtypeidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("roomtypeid");
                    _dirtyCount++;
                }

                if (pObject.isAmenityidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("amenityid");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isRoomtypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
                }
    
                if (pObject.isAmenityidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE roomtypeamenity SET ");
                boolean useComma=false;

                if (pObject.isRoomtypeidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("roomtypeid").append("=?");
                }

                if (pObject.isAmenityidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("amenityid").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("roomtypeamenity.roomtypeid=? AND roomtypeamenity.amenityid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isRoomtypeidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
                }

                if (pObject.isAmenityidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
                Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of RoomtypeamenityBean pObjects into the database.
     *
     * @param pObjects the RoomtypeamenityBean pObject table to be saved
     * @return the saved RoomtypeamenityBean array.
     */
    //65
    public RoomtypeamenityBean[] save(RoomtypeamenityBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique RoomtypeamenityBean pObject from a template one giving a c
     *
     * @param pObject the RoomtypeamenityBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public RoomtypeamenityBean loadUniqueUsingTemplate(RoomtypeamenityBean pObject) throws SQLException
    {
         RoomtypeamenityBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of RoomtypeamenityBean from a template one.
     *
     * @param pObject the RoomtypeamenityBean template to look for
     * @return all the RoomtypeamenityBean matching the template
     */
    //88
    public RoomtypeamenityBean[] loadUsingTemplate(RoomtypeamenityBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from roomtypeamenity WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isRoomtypeidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("roomtypeid= ?");
             }
    
             if (pObject.isAmenityidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("amenityid= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isRoomtypeidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
             }
    
             if (pObject.isAmenityidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a RoomtypeamenityBean template.
     *
     * @param pObject the RoomtypeamenityBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(RoomtypeamenityBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM roomtypeamenity WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isRoomtypeidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("roomtypeid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isAmenityidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("amenityid").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isRoomtypeidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
            }
    
            if (pObject.isAmenityidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table roomtypeamenity.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table roomtypeamenity with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from roomtypeamenity " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table roomtypeamenity with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific RoomtypeamenityBean pObject given a c
     *
     * @param pObject the RoomtypeamenityBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(RoomtypeamenityBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from roomtypeamenity WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isRoomtypeidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("roomtypeid= ?");
                }
    
                if (pObject.isAmenityidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("amenityid= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isRoomtypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getRoomtypeid());
                }
    
                if (pObject.isAmenityidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getAmenityid());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the roomtypeamenity on a RoomtypeamenityBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting RoomtypeamenityBean pObject
     */
    //72
    public RoomtypeamenityBean decodeRow(ResultSet rs) throws SQLException
    {
        RoomtypeamenityBean pObject = createRoomtypeamenityBean();
        pObject.setRoomtypeid(Manager.getInteger(rs, 1));
        pObject.setAmenityid(Manager.getInteger(rs, 2));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the roomtypeamenity table on a RoomtypeamenityBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting RoomtypeamenityBean pObject
     */
    //73
    public RoomtypeamenityBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        RoomtypeamenityBean pObject = createRoomtypeamenityBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_ROOMTYPEID:
                    ++pos;
                    pObject.setRoomtypeid(Manager.getInteger(rs, pos));
                    break;
                case ID_AMENITYID:
                    ++pos;
                    pObject.setAmenityid(Manager.getInteger(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of RoomtypeamenityBean 
     */
    //41
    public RoomtypeamenityBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of RoomtypeamenityBean 
     */
    public RoomtypeamenityBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (RoomtypeamenityBean[])v.toArray(new RoomtypeamenityBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private RoomtypeamenityListener listener = null;

    /**
     * Registers a unique RoomtypeamenityListener listener.
     */
    //66.5
    public void registerListener(RoomtypeamenityListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the RoomtypeamenityBean pObject.
     *
     * @param pObject the RoomtypeamenityBean pObject to be saved
     */
    //67
    void beforeInsert(RoomtypeamenityBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the RoomtypeamenityBean pObject.
     *
     * @param pObject the RoomtypeamenityBean pObject to be saved
     */
    //68
    void afterInsert(RoomtypeamenityBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the RoomtypeamenityBean pObject.
     *
     * @param pObject the RoomtypeamenityBean pObject to be updated
     */
    //69
    void beforeUpdate(RoomtypeamenityBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the RoomtypeamenityBean pObject.
     *
     * @param pObject the RoomtypeamenityBean pObject to be updated
     */
    //70
    void afterUpdate(RoomtypeamenityBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 

}
