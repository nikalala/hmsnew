// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.util.Comparator;
// imports+ 

// imports- 


/**
 * Comparator class is used to sort the AmenityBean objects.
 */
public class AmenityComparator implements Comparator
// extends+ 

// extends- 

{
    /**
     * Holds the field on which the comparison is performed.
     */
    private int iType;
    /**
     * Value that will contain the information about the order of the sort: normal or reversal.
     */
    private boolean bReverse;

    /**
     * Constructor class for AmenityComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new AmenityComparator(AmenityManager.AMENITYID, bReverse));<code>
     *
     * @param iType the field from which you want to sort
     * <br>
     * Possible values are:
     * <ul>
     *   <li>AmenityManager.ID_AMENITYID
     *   <li>AmenityManager.ID_NAME
     *   <li>AmenityManager.ID_TYPE
     *   <li>AmenityManager.ID_ORD
     *   <li>AmenityManager.ID_REGBYID
     *   <li>AmenityManager.ID_REGDATE
     * </ul>
     */
    public AmenityComparator(int iType)
    {
        this(iType, false);
    }

    /**
     * Constructor class for AmenityComparator.
     * <br>
     * Example:
     * <br>
     * <code>Arrays.sort(pArray, new AmenityComparator(AmenityManager.AMENITYID, bReverse));<code>
     *
     * @param iType the field from which you want to sort.
     * <br>
     * Possible values are:
     * <ul>
     *   <li>AmenityManager.ID_AMENITYID
     *   <li>AmenityManager.ID_NAME
     *   <li>AmenityManager.ID_TYPE
     *   <li>AmenityManager.ID_ORD
     *   <li>AmenityManager.ID_REGBYID
     *   <li>AmenityManager.ID_REGDATE
     *   <li>AmenityManager.ID_ACTIVE
     *   <li>AmenityManager.ID_DELETED
     * </ul>
     *
     * @param bReverse set this value to true, if you want to reverse the sorting results
     */
    public AmenityComparator(int iType, boolean bReverse)
    {
        this.iType = iType;
        this.bReverse = bReverse;
    }

    /**
     * Implementation of the compare method.
     */
    public int compare(Object pObj1, Object pObj2)
    {
        AmenityBean b1 = (AmenityBean)pObj1;
        AmenityBean b2 = (AmenityBean)pObj2;
        int iReturn = 0;
        switch(iType)
        {
            case AmenityManager.ID_AMENITYID:
                if (b1.getAmenityid() == null && b2.getAmenityid() != null) {
                    iReturn = -1;
                } else if (b1.getAmenityid() == null && b2.getAmenityid() == null) {
                    iReturn = 0;
                } else if (b1.getAmenityid() != null && b2.getAmenityid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getAmenityid().compareTo(b2.getAmenityid());
                }
                break;
            case AmenityManager.ID_NAME:
                if (b1.getName() == null && b2.getName() != null) {
                    iReturn = -1;
                } else if (b1.getName() == null && b2.getName() == null) {
                    iReturn = 0;
                } else if (b1.getName() != null && b2.getName() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getName().compareTo(b2.getName());
                }
                break;
            case AmenityManager.ID_TYPE:
                if (b1.getType() == null && b2.getType() != null) {
                    iReturn = -1;
                } else if (b1.getType() == null && b2.getType() == null) {
                    iReturn = 0;
                } else if (b1.getType() != null && b2.getType() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getType().compareTo(b2.getType());
                }
                break;
            case AmenityManager.ID_ORD:
                if (b1.getOrd() == null && b2.getOrd() != null) {
                    iReturn = -1;
                } else if (b1.getOrd() == null && b2.getOrd() == null) {
                    iReturn = 0;
                } else if (b1.getOrd() != null && b2.getOrd() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getOrd().compareTo(b2.getOrd());
                }
                break;
            case AmenityManager.ID_REGBYID:
                if (b1.getRegbyid() == null && b2.getRegbyid() != null) {
                    iReturn = -1;
                } else if (b1.getRegbyid() == null && b2.getRegbyid() == null) {
                    iReturn = 0;
                } else if (b1.getRegbyid() != null && b2.getRegbyid() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getRegbyid().compareTo(b2.getRegbyid());
                }
                break;
            case AmenityManager.ID_REGDATE:
                if (b1.getRegdate() == null && b2.getRegdate() != null) {
                    iReturn = -1;
                } else if (b1.getRegdate() == null && b2.getRegdate() == null) {
                    iReturn = 0;
                } else if (b1.getRegdate() != null && b2.getRegdate() == null) {
                    iReturn = 1;
                } else { 
                    iReturn = b1.getRegdate().compareTo(b2.getRegdate());
                }
                break;
            default: 
                throw new IllegalArgumentException("Type passed for the field is not supported");
        }

        return bReverse ? (-1 * iReturn) : iReturn;
    }
// class+ 

// class- 

}
