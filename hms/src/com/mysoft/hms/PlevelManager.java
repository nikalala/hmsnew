// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.sql.*;
// imports+ 

// imports- 


/**
 * Handles database calls for the plevel table.
 */
public class PlevelManager
// extends+ 

// extends- 

{

    /**
     * Column plevelid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_PLEVELID = 0;
    public static final int TYPE_PLEVELID = Types.INTEGER;
    public static final String NAME_PLEVELID = "plevelid";

    /**
     * Column personneltypeid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_PERSONNELTYPEID = 1;
    public static final int TYPE_PERSONNELTYPEID = Types.INTEGER;
    public static final String NAME_PERSONNELTYPEID = "personneltypeid";

    /**
     * Column menuid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_MENUID = 2;
    public static final int TYPE_MENUID = Types.INTEGER;
    public static final String NAME_MENUID = "menuid";

    /**
     * Column read of type Types.BIT mapped to Boolean.
     */
    public static final int ID_READ = 3;
    public static final int TYPE_READ = Types.BIT;
    public static final String NAME_READ = "read";

    /**
     * Column write of type Types.BIT mapped to Boolean.
     */
    public static final int ID_WRITE = 4;
    public static final int TYPE_WRITE = Types.BIT;
    public static final String NAME_WRITE = "write";


    private static final String TABLE_NAME = "plevel";

    /**
     * Create an array of type string containing all the fields of the plevel table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "plevel.plevelid"
        ,"plevel.personneltypeid"
        ,"plevel.menuid"
        ,"plevel.read"
        ,"plevel.write"
    };

    /**
     * Field that contains the comma separated fields of the plevel table.
     */
    private static final String ALL_FIELDS = "plevel.plevelid"
                            + ",plevel.personneltypeid"
                            + ",plevel.menuid"
                            + ",plevel.read"
                            + ",plevel.write";

    private static PlevelManager singleton = new PlevelManager();

    /**
     * Get the PlevelManager singleton.
     *
     * @return PlevelManager 
     */
    synchronized public static PlevelManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own PlevelManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(PlevelManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new PlevelBean instance.
     *
     * @return the new PlevelBean 
     */
    public PlevelBean createPlevelBean()
    {
        return new PlevelBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a PlevelBean from the plevel using its key fields.
     *
     * @return a unique PlevelBean 
     */
    //12
    public PlevelBean loadByPrimaryKey(Integer plevelid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM plevel WHERE plevel.plevelid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, plevelid);
            PlevelBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Integer plevelid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from plevel WHERE plevel.plevelid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, plevelid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads PlevelBean array from the plevel table using its menuid field.
     *
     * @return an array of PlevelBean 
     */
    // LOAD BY IMPORTED KEY
    public PlevelBean[] loadByMenuid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM plevel WHERE menuid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the plevel table by menuid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByMenuid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM plevel WHERE menuid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Loads PlevelBean array from the plevel table using its personneltypeid field.
     *
     * @return an array of PlevelBean 
     */
    // LOAD BY IMPORTED KEY
    public PlevelBean[] loadByPersonneltypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM plevel WHERE personneltypeid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the plevel table by personneltypeid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByPersonneltypeid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM plevel WHERE personneltypeid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the MenuBean object from the plevel.menuid field.
     *
     * @param pObject the PlevelBean 
     * @return the associated MenuBean pObject
     */
    // GET IMPORTED
    public MenuBean getMenuBean(PlevelBean pObject) throws SQLException
    {
        MenuBean other = MenuManager.getInstance().createMenuBean();
        other.setMenuid(pObject.getMenuid());
        return MenuManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the PlevelBean object to the MenuBean object.
     *
     * @param pObject the PlevelBean object to use
     * @param pObjectToBeSet the MenuBean object to associate to the PlevelBean 
     * @return the associated MenuBean pObject
     */
    // SET IMPORTED
    public PlevelBean setMenuBean(PlevelBean pObject,MenuBean pObjectToBeSet)
    {
        pObject.setMenuid(pObjectToBeSet.getMenuid());
        return pObject;
    }

    /**
     * Retrieves the PersonneltypeBean object from the plevel.personneltypeid field.
     *
     * @param pObject the PlevelBean 
     * @return the associated PersonneltypeBean pObject
     */
    // GET IMPORTED
    public PersonneltypeBean getPersonneltypeBean(PlevelBean pObject) throws SQLException
    {
        PersonneltypeBean other = PersonneltypeManager.getInstance().createPersonneltypeBean();
        other.setPersonneltypeid(pObject.getPersonneltypeid());
        return PersonneltypeManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the PlevelBean object to the PersonneltypeBean object.
     *
     * @param pObject the PlevelBean object to use
     * @param pObjectToBeSet the PersonneltypeBean object to associate to the PlevelBean 
     * @return the associated PersonneltypeBean pObject
     */
    // SET IMPORTED
    public PlevelBean setPersonneltypeBean(PlevelBean pObject,PersonneltypeBean pObjectToBeSet)
    {
        pObject.setPersonneltypeid(pObjectToBeSet.getPersonneltypeid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from plevel.
     *
     * @return an array of PlevelManager pObject
     */
    //38
    public PlevelBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM plevel",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of PlevelBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting PlevelBean table 
     */
    //49
    public PlevelBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of PlevelBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting PlevelBean table 
     */
    //51
    public PlevelBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from plevel " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from plevel ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (PlevelBean[])v.toArray(new PlevelBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from plevel table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the plevel table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM plevel " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the PlevelBean pObject into the database.
     *
     * @param pObject the PlevelBean pObject to be saved
     */
    //100
    public PlevelBean save(PlevelBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                if (!pObject.isPlevelidModified())
                {
                    ps = c.prepareStatement("SELECT nextval('plevelid_seq')");
                    ResultSet rs = null;
                    try
                    {
                        rs = ps.executeQuery();
                        if(rs.next())
                            pObject.setPlevelid(Manager.getInteger(rs, 1));
                        else
                            getManager().log("ATTENTION: Could not retrieve generated key!");
                    }
                    finally
                    {
                        getManager().close(ps, rs);
                        ps=null;
                    }
                }
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into plevel (");
    
                if (pObject.isPlevelidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("plevelid");
                    _dirtyCount++;
                }

                if (pObject.isPersonneltypeidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("personneltypeid");
                    _dirtyCount++;
                }

                if (pObject.isMenuidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("menuid");
                    _dirtyCount++;
                }

                if (pObject.isReadModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("read");
                    _dirtyCount++;
                }

                if (pObject.isWriteModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("write");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isPlevelidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
                }
    
                if (pObject.isPersonneltypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getPersonneltypeid());
                }
    
                if (pObject.isMenuidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }
    
                if (pObject.isReadModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getRead());
                }
    
                if (pObject.isWriteModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getWrite());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE plevel SET ");
                boolean useComma=false;

                if (pObject.isPlevelidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("plevelid").append("=?");
                }

                if (pObject.isPersonneltypeidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("personneltypeid").append("=?");
                }

                if (pObject.isMenuidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("menuid").append("=?");
                }

                if (pObject.isReadModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("read").append("=?");
                }

                if (pObject.isWriteModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("write").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("plevel.plevelid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isPlevelidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
                }

                if (pObject.isPersonneltypeidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getPersonneltypeid());
                }

                if (pObject.isMenuidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }

                if (pObject.isReadModified()) {
                      Manager.setBoolean(ps, ++_dirtyCount, pObject.getRead());
                }

                if (pObject.isWriteModified()) {
                      Manager.setBoolean(ps, ++_dirtyCount, pObject.getWrite());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of PlevelBean pObjects into the database.
     *
     * @param pObjects the PlevelBean pObject table to be saved
     * @return the saved PlevelBean array.
     */
    //65
    public PlevelBean[] save(PlevelBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique PlevelBean pObject from a template one giving a c
     *
     * @param pObject the PlevelBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public PlevelBean loadUniqueUsingTemplate(PlevelBean pObject) throws SQLException
    {
         PlevelBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of PlevelBean from a template one.
     *
     * @param pObject the PlevelBean template to look for
     * @return all the PlevelBean matching the template
     */
    //88
    public PlevelBean[] loadUsingTemplate(PlevelBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from plevel WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isPlevelidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("plevelid= ?");
             }
    
             if (pObject.isPersonneltypeidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("personneltypeid= ?");
             }
    
             if (pObject.isMenuidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("menuid= ?");
             }
    
             if (pObject.isReadModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("read= ?");
             }
    
             if (pObject.isWriteModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("write= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isPlevelidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
             }
    
             if (pObject.isPersonneltypeidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getPersonneltypeid());
             }
    
             if (pObject.isMenuidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
             }
    
             if (pObject.isReadModified()) {
                 Manager.setBoolean(ps, ++_dirtyCount, pObject.getRead());
             }
    
             if (pObject.isWriteModified()) {
                 Manager.setBoolean(ps, ++_dirtyCount, pObject.getWrite());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a PlevelBean template.
     *
     * @param pObject the PlevelBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(PlevelBean pObject) throws SQLException
    {
        if (pObject.isPlevelidInitialized())
            return deleteByPrimaryKey(pObject.getPlevelid());
    
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM plevel WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isPlevelidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("plevelid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isPersonneltypeidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("personneltypeid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isMenuidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("menuid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isReadInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("read").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isWriteInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("write").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isPlevelidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
            }
    
            if (pObject.isPersonneltypeidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getPersonneltypeid());
            }
    
            if (pObject.isMenuidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
            }
    
            if (pObject.isReadInitialized()) {
                Manager.setBoolean(ps, ++_dirtyCount, pObject.getRead());
            }
    
            if (pObject.isWriteInitialized()) {
                Manager.setBoolean(ps, ++_dirtyCount, pObject.getWrite());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table plevel.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table plevel with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from plevel " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table plevel with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific PlevelBean pObject given a c
     *
     * @param pObject the PlevelBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(PlevelBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from plevel WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isPlevelidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("plevelid= ?");
                }
    
                if (pObject.isPersonneltypeidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("personneltypeid= ?");
                }
    
                if (pObject.isMenuidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("menuid= ?");
                }
    
                if (pObject.isReadModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("read= ?");
                }
    
                if (pObject.isWriteModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("write= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isPlevelidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getPlevelid());
                }
    
                if (pObject.isPersonneltypeidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getPersonneltypeid());
                }
    
                if (pObject.isMenuidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getMenuid());
                }
    
                if (pObject.isReadModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getRead());
                }
    
                if (pObject.isWriteModified()) {
                    Manager.setBoolean(ps, ++_dirtyCount, pObject.getWrite());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the plevel on a PlevelBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting PlevelBean pObject
     */
    //72
    public PlevelBean decodeRow(ResultSet rs) throws SQLException
    {
        PlevelBean pObject = createPlevelBean();
        pObject.setPlevelid(Manager.getInteger(rs, 1));
        pObject.setPersonneltypeid(Manager.getInteger(rs, 2));
        pObject.setMenuid(Manager.getInteger(rs, 3));
        pObject.setRead(Manager.getBoolean(rs, 4));
        pObject.setWrite(Manager.getBoolean(rs, 5));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the plevel table on a PlevelBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting PlevelBean pObject
     */
    //73
    public PlevelBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        PlevelBean pObject = createPlevelBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_PLEVELID:
                    ++pos;
                    pObject.setPlevelid(Manager.getInteger(rs, pos));
                    break;
                case ID_PERSONNELTYPEID:
                    ++pos;
                    pObject.setPersonneltypeid(Manager.getInteger(rs, pos));
                    break;
                case ID_MENUID:
                    ++pos;
                    pObject.setMenuid(Manager.getInteger(rs, pos));
                    break;
                case ID_READ:
                    ++pos;
                    pObject.setRead(Manager.getBoolean(rs, pos));
                    break;
                case ID_WRITE:
                    ++pos;
                    pObject.setWrite(Manager.getBoolean(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of PlevelBean 
     */
    //41
    public PlevelBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of PlevelBean 
     */
    public PlevelBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (PlevelBean[])v.toArray(new PlevelBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private PlevelListener listener = null;

    /**
     * Registers a unique PlevelListener listener.
     */
    //66.5
    public void registerListener(PlevelListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the PlevelBean pObject.
     *
     * @param pObject the PlevelBean pObject to be saved
     */
    //67
    void beforeInsert(PlevelBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the PlevelBean pObject.
     *
     * @param pObject the PlevelBean pObject to be saved
     */
    //68
    void afterInsert(PlevelBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the PlevelBean pObject.
     *
     * @param pObject the PlevelBean pObject to be updated
     */
    //69
    void beforeUpdate(PlevelBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the PlevelBean pObject.
     *
     * @param pObject the PlevelBean pObject to be updated
     */
    //70
    void afterUpdate(PlevelBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 

}
