// --------------------------------------------------------
// Generated by sql2java - http://sql2java.sourceforge.net/
// jdbc driver used at code generation time: org.postgresql.Driver
// 
// Please help us improve this tool by reporting:
//    problems,
//    suggestions,
//    feedbacks,
//    ideas,
//  to http://sourceforge.net/projects/sql2java/
// --------------------------------------------------------

package com.mysoft.hms;

import java.sql.*;
// imports+ 

// imports- 


/**
 * Handles database calls for the taxsequence table.
 */
public class TaxsequenceManager
// extends+ 

// extends- 

{

    /**
     * Column taxid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_TAXID = 0;
    public static final int TYPE_TAXID = Types.INTEGER;
    public static final String NAME_TAXID = "taxid";

    /**
     * Column afterid of type Types.INTEGER mapped to Integer.
     */
    public static final int ID_AFTERID = 1;
    public static final int TYPE_AFTERID = Types.INTEGER;
    public static final String NAME_AFTERID = "afterid";


    private static final String TABLE_NAME = "taxsequence";

    /**
     * Create an array of type string containing all the fields of the taxsequence table.
     */
    private static final String[] FIELD_NAMES = 
    {
        "taxsequence.taxid"
        ,"taxsequence.afterid"
    };

    /**
     * Field that contains the comma separated fields of the taxsequence table.
     */
    private static final String ALL_FIELDS = "taxsequence.taxid"
                            + ",taxsequence.afterid";

    private static TaxsequenceManager singleton = new TaxsequenceManager();

    /**
     * Get the TaxsequenceManager singleton.
     *
     * @return TaxsequenceManager 
     */
    synchronized public static TaxsequenceManager getInstance()
    {
        return singleton;
    }

    /**
     * Sets your own TaxsequenceManager instance.
     <br>
     * This is optional, by default we provide it for you.
     */
    synchronized public static void setInstance(TaxsequenceManager instance)
    {
        singleton = instance;
    }


    /**
     * Creates a new TaxsequenceBean instance.
     *
     * @return the new TaxsequenceBean 
     */
    public TaxsequenceBean createTaxsequenceBean()
    {
        return new TaxsequenceBean();
    }

    //////////////////////////////////////
    // PRIMARY KEY METHODS
    //////////////////////////////////////

    /**
     * Loads a TaxsequenceBean from the taxsequence using its key fields.
     *
     * @return a unique TaxsequenceBean 
     */
    //12
    public TaxsequenceBean loadByPrimaryKey(Integer taxid, Integer afterid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM taxsequence WHERE taxsequence.taxid=? and taxsequence.afterid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, taxid);
            Manager.setInteger(ps, 2, afterid);
            TaxsequenceBean pReturn[] = loadByPreparedStatement(ps);
            if (pReturn.length < 1)
                return null;
            else
                return pReturn[0];
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    /**
     * Deletes rows according to its keys.
     *
     * @return the number of deleted rows
     */
    //60
    public int deleteByPrimaryKey(Integer taxid, Integer afterid) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        try
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE from taxsequence WHERE taxsequence.taxid=? and taxsequence.afterid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, taxid);
            Manager.setInteger(ps, 2, afterid);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    
    
    //////////////////////////////////////
    // FOREIGN KEY METHODS 
    //////////////////////////////////////

    /**
     * Loads TaxsequenceBean array from the taxsequence table using its afterid field.
     *
     * @return an array of TaxsequenceBean 
     */
    // LOAD BY IMPORTED KEY
    public TaxsequenceBean[] loadByAfterid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM taxsequence WHERE afterid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the taxsequence table by afterid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByAfterid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM taxsequence WHERE afterid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Loads TaxsequenceBean array from the taxsequence table using its taxid field.
     *
     * @return an array of TaxsequenceBean 
     */
    // LOAD BY IMPORTED KEY
    public TaxsequenceBean[] loadByTaxid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM taxsequence WHERE taxid=?",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            Manager.setInteger(ps, 1, value);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }


    /**
     * Deletes from the taxsequence table by taxid field.
     *
     * @param value the key value to seek
     * @return the number of rows deleted
     */
    // DELETE BY IMPORTED KEY
    public int deleteByTaxid(Integer value) throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("DELETE FROM taxsequence WHERE taxid=?");
            Manager.setInteger(ps, 1, value);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    //////////////////////////////////////
    // GET/SET FOREIGN KEY BEAN METHOD
    //////////////////////////////////////
    /**
     * Retrieves the TaxBean object from the taxsequence.taxid field.
     *
     * @param pObject the TaxsequenceBean 
     * @return the associated TaxBean pObject
     */
    // GET IMPORTED
    public TaxBean getTaxBean(TaxsequenceBean pObject) throws SQLException
    {
        TaxBean other = TaxManager.getInstance().createTaxBean();
        other.setTaxid(pObject.getAfterid());
        return TaxManager.getInstance().loadUniqueUsingTemplate(other);
    }

    /**
     * Associates the TaxsequenceBean object to the TaxBean object.
     *
     * @param pObject the TaxsequenceBean object to use
     * @param pObjectToBeSet the TaxBean object to associate to the TaxsequenceBean 
     * @return the associated TaxBean pObject
     */
    // SET IMPORTED
    public TaxsequenceBean setTaxBean(TaxsequenceBean pObject,TaxBean pObjectToBeSet)
    {
        pObject.setAfterid(pObjectToBeSet.getTaxid());
        return pObject;
    }



    //////////////////////////////////////
    // LOAD ALL
    //////////////////////////////////////

    /**
     * Loads all the rows from taxsequence.
     *
     * @return an array of TaxsequenceManager pObject
     */
    //38
    public TaxsequenceBean[] loadAll() throws SQLException 
    {
        Connection c = null;
        PreparedStatement ps = null;
        try 
        {
            c = getConnection();
            ps = c.prepareStatement("SELECT " + ALL_FIELDS + " FROM taxsequence",ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }

    //////////////////////////////////////
    // SQL 'WHERE' METHOD
    //////////////////////////////////////
    /**
     * Retrieves an array of TaxsequenceBean given a sql 'where' clause.
     *
     * @param where the sql 'where' clause
     * @return the resulting TaxsequenceBean table 
     */
    //49
    public TaxsequenceBean[] loadByWhere(String where) throws SQLException
    {
        return loadByWhere(where, null);
    }

    /**
     * Retrieves an array of TaxsequenceBean given a sql where clause, and a list of fields.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the sql 'where' clause
     * @param fieldList table of the field's associated constants
     * @return the resulting TaxsequenceBean table 
     */
    //51
    public TaxsequenceBean[] loadByWhere(String where, int[] fieldList) throws SQLException
    {
        String sql = null;
        if(fieldList == null)
            sql = "select " + ALL_FIELDS + " from taxsequence " + where;
        else
        {
            StringBuffer buff = new StringBuffer(128);
            buff.append("select ");
            for(int i = 0; i < fieldList.length; i++)
            {
                if(i != 0)
                    buff.append(",");
                buff.append(FIELD_NAMES[fieldList[i]]);
            }
            buff.append(" from taxsequence ");
            buff.append(where);
            sql = buff.toString();
            buff = null;
        }
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        java.util.ArrayList v = null;
        try 
        {
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else
                    v.add(decodeRow(rs, fieldList));
            }

            return (TaxsequenceBean[])v.toArray(new TaxsequenceBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); }
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
    }


    /**
     * Deletes all rows from taxsequence table.
     * @return the number of deleted rows.
     */
    public int deleteAll() throws SQLException
    {
        return deleteByWhere("");
    }


    /**
     * Deletes rows from the taxsequence table using a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     * <br>Attention, if 'WHERE' is omitted it will delete all records. 
     *
     * @param where the sql 'where' clause
     * @return the number of deleted rows
     */
    public int deleteByWhere(String where) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;

        try
        {
            c = getConnection();
            String delByWhereSQL = "DELETE FROM taxsequence " + where;
            ps = c.prepareStatement(delByWhereSQL);
            return ps.executeUpdate();
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // SAVE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Saves the TaxsequenceBean pObject into the database.
     *
     * @param pObject the TaxsequenceBean pObject to be saved
     */
    //100
    public TaxsequenceBean save(TaxsequenceBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;

        try
        {
            c = getConnection();
            if (pObject.isNew())
            { // SAVE 
                beforeInsert(pObject); // listener callback
                int _dirtyCount = 0;
                _sql = new StringBuffer("INSERT into taxsequence (");
    
                if (pObject.isTaxidModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("taxid");
                    _dirtyCount++;
                }

                if (pObject.isAfteridModified()) {
                    if (_dirtyCount>0) {
                        _sql.append(",");
                    }
                    _sql.append("afterid");
                    _dirtyCount++;
                }

                _sql.append(") values (");
                if(_dirtyCount > 0) {
                    _sql.append("?");
                    for(int i = 1; i < _dirtyCount; i++) {
                        _sql.append(",?");
                    }
                }
                _sql.append(")");

                ps = c.prepareStatement(_sql.toString(), ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                _dirtyCount = 0;

                if (pObject.isTaxidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
                }
    
                if (pObject.isAfteridModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
                }
    
                ps.executeUpdate();
    
                pObject.isNew(false);
                pObject.resetIsModified();
                afterInsert(pObject); // listener callback
            }
            else 
            { // UPDATE 
                beforeUpdate(pObject); // listener callback
                _sql = new StringBuffer("UPDATE taxsequence SET ");
                boolean useComma=false;

                if (pObject.isTaxidModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("taxid").append("=?");
                }

                if (pObject.isAfteridModified()) {
                    if (useComma) {
                        _sql.append(",");
                    } else {
                        useComma=true;
                    }
                    _sql.append("afterid").append("=?");
                }
                _sql.append(" WHERE ");
                _sql.append("taxsequence.taxid=? AND taxsequence.afterid=?");
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
                int _dirtyCount = 0;

                if (pObject.isTaxidModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
                }

                if (pObject.isAfteridModified()) {
                      Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
                }
    
                if (_dirtyCount == 0) {
                     return pObject;
                }
    
                Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
                Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
                ps.executeUpdate();
                pObject.resetIsModified();
                afterUpdate(pObject); // listener callback
            }
    
            return pObject;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    /**
     * Saves an array of TaxsequenceBean pObjects into the database.
     *
     * @param pObjects the TaxsequenceBean pObject table to be saved
     * @return the saved TaxsequenceBean array.
     */
    //65
    public TaxsequenceBean[] save(TaxsequenceBean[] pObjects) throws SQLException 
    {
        for (int iIndex = 0; iIndex < pObjects.length; iIndex ++){
            save(pObjects[iIndex]);
        }
        return pObjects;
    }



    ///////////////////////////////////////////////////////////////////////
    // USING TEMPLATE 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Loads a unique TaxsequenceBean pObject from a template one giving a c
     *
     * @param pObject the TaxsequenceBean pObject to look for
     * @return the pObject matching the template
     */
    //85
    public TaxsequenceBean loadUniqueUsingTemplate(TaxsequenceBean pObject) throws SQLException
    {
         TaxsequenceBean[] pReturn = loadUsingTemplate(pObject);
         if (pReturn.length == 0)
             return null;
         if (pReturn.length > 1)
             throw new SQLException("More than one element !!");
         return pReturn[0];
     }

    /**
     * Loads an array of TaxsequenceBean from a template one.
     *
     * @param pObject the TaxsequenceBean template to look for
     * @return all the TaxsequenceBean matching the template
     */
    //88
    public TaxsequenceBean[] loadUsingTemplate(TaxsequenceBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer where = new StringBuffer("");
        StringBuffer _sql = new StringBuffer("SELECT " + ALL_FIELDS + " from taxsequence WHERE ");
        StringBuffer _sqlWhere = new StringBuffer("");
        try
        {
            int _dirtyCount = 0;
    
             if (pObject.isTaxidModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("taxid= ?");
             }
    
             if (pObject.isAfteridModified()) {
                 _dirtyCount ++; 
                 _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("afterid= ?");
             }
    
             if (_dirtyCount == 0) {
                 throw new SQLException ("The pObject to look for is invalid : not initialized !");
             }
             _sql.append(_sqlWhere);
             c = getConnection();
             ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
             _dirtyCount = 0;
    
             if (pObject.isTaxidModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
             }
    
             if (pObject.isAfteridModified()) {
                 Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
             }
    
             ps.executeQuery();
             return loadByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }
    /**
     * Deletes rows using a TaxsequenceBean template.
     *
     * @param pObject the TaxsequenceBean object(s) to be deleted
     * @return the number of deleted objects
     */
    //63
    public int deleteUsingTemplate(TaxsequenceBean pObject) throws SQLException
    {
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer sql = null;
    
        try 
        {
            sql = new StringBuffer("DELETE FROM taxsequence WHERE ");
            int _dirtyAnd = 0;
            if (pObject.isTaxidInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("taxid").append("=?");
                _dirtyAnd ++;
            }
    
            if (pObject.isAfteridInitialized()) {
                if (_dirtyAnd > 0)
                    sql.append(" AND ");
                sql.append("afterid").append("=?");
                _dirtyAnd ++;
            }
    
            c = getConnection();
            ps = c.prepareStatement(sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
            int _dirtyCount = 0;
    
            if (pObject.isTaxidInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
            }
    
            if (pObject.isAfteridInitialized()) {
                Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
            }
    
            int _rows = ps.executeUpdate();
            return _rows;
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // COUNT 
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the number of rows of the table taxsequence.
     *
     * @return the number of rows returned
     */
    //78
    public int countAll() throws SQLException
    {
        return countWhere("");
    }



    /**
     * Retrieves the number of rows of the table taxsequence with a 'where' clause.
     * It is up to you to pass the 'WHERE' in your where clausis.
     *
     * @param where the restriction clause
     * @return the number of rows returned
     */
    public int countWhere(String where) throws SQLException
    {
        String sql = "select count(*) as MCOUNT from taxsequence " + where;
        Connection c = null;
        Statement pStatement = null;
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;    
            c = getConnection();
            pStatement = c.createStatement();
            rs =  pStatement.executeQuery(sql);
            if (rs.next())
            {
                iReturn = rs.getInt("MCOUNT");
            }
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(pStatement, rs);
            freeConnection(c);
        }
       throw new SQLException("Error in countWhere");
    }

    /**
     * Retrieves the number of rows of the table taxsequence with a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return the number of rows returned
     */
    //82
    int countByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        ResultSet rs =  null;
        try 
        {
            int iReturn = -1;
            rs = ps.executeQuery();
            if (rs.next())
                iReturn = rs.getInt("MCOUNT");
            if (iReturn != -1)
                return iReturn;
        }
        finally
        {
            getManager().close(rs);
        }
       throw new SQLException("Error in countByPreparedStatement");
    }

    /**
     * Looks for the number of elements of a specific TaxsequenceBean pObject given a c
     *
     * @param pObject the TaxsequenceBean pObject to look for
     * @return the number of rows returned
     */
    //83
    public int countUsingTemplate(TaxsequenceBean pObject) throws SQLException
    {
        StringBuffer where = new StringBuffer("");
        Connection c = null;
        PreparedStatement ps = null;
        StringBuffer _sql = null;
        StringBuffer _sqlWhere = null;
    
        try
        {
                _sql = new StringBuffer("SELECT count(*) as MCOUNT  from taxsequence WHERE ");
                _sqlWhere = new StringBuffer("");
                int _dirtyCount = 0;
    
                if (pObject.isTaxidModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("taxid= ?");
                }
    
                if (pObject.isAfteridModified()) {
                    _dirtyCount++; 
                    _sqlWhere.append((_sqlWhere.length() == 0) ? " " : " AND ").append("afterid= ?");
                }
    
                if (_dirtyCount == 0)
                   throw new SQLException ("The pObject to look is unvalid : not initialized !");
    
                _sql.append(_sqlWhere);
                c = getConnection();
                ps = c.prepareStatement(_sql.toString(),ResultSet.TYPE_SCROLL_INSENSITIVE, ResultSet.CONCUR_READ_ONLY);
    
                _dirtyCount = 0;
    
                if (pObject.isTaxidModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getTaxid());
                }
    
                if (pObject.isAfteridModified()) {
                    Manager.setInteger(ps, ++_dirtyCount, pObject.getAfterid());
                }
    
                return countByPreparedStatement(ps);
        }
        finally
        {
            getManager().close(ps);
            freeConnection(c);
        }
    }



    ///////////////////////////////////////////////////////////////////////
    // DECODE RESULT SET 
    ///////////////////////////////////////////////////////////////////////
    /**
     * Transforms a ResultSet iterating on the taxsequence on a TaxsequenceBean pObject.
     *
     * @param rs the ResultSet to be transformed
     * @return pObject resulting TaxsequenceBean pObject
     */
    //72
    public TaxsequenceBean decodeRow(ResultSet rs) throws SQLException
    {
        TaxsequenceBean pObject = createTaxsequenceBean();
        pObject.setTaxid(Manager.getInteger(rs, 1));
        pObject.setAfterid(Manager.getInteger(rs, 2));

        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    /**
     * Transforms a ResultSet iterating on the taxsequence table on a TaxsequenceBean pObject according to a list of fields.
     *
     * @param rs the ResultSet to be transformed
     * @param fieldList table of the field's associated constants
     * @return pObject resulting TaxsequenceBean pObject
     */
    //73
    public TaxsequenceBean decodeRow(ResultSet rs, int[] fieldList) throws SQLException
    {
        TaxsequenceBean pObject = createTaxsequenceBean();
        int pos = 0;
        for(int i = 0; i < fieldList.length; i++)
        {
            switch(fieldList[i]) {
                case ID_TAXID:
                    ++pos;
                    pObject.setTaxid(Manager.getInteger(rs, pos));
                    break;
                case ID_AFTERID:
                    ++pos;
                    pObject.setAfterid(Manager.getInteger(rs, pos));
                    break;
            }
        }
        pObject.isNew(false);
        pObject.resetIsModified();

        return pObject;
    }

    //////////////////////////////////////
    // PREPARED STATEMENT LOADER
    //////////////////////////////////////

    /**
     * Loads all the elements using a prepared statement.
     *
     * @param ps the PreparedStatement to be used
     * @return an array of TaxsequenceBean 
     */
    //41
    public TaxsequenceBean[] loadByPreparedStatement(PreparedStatement ps) throws SQLException
    {
        return loadByPreparedStatement(ps, null);
    }

    /**
     * Loads all the elements using a prepared statement specifying a list of fields to be retrieved.
     *
     * @param ps the PreparedStatement to be used
     * @param fieldList table of the field's associated constants
     * @return an array of TaxsequenceBean 
     */
    public TaxsequenceBean[] loadByPreparedStatement(PreparedStatement ps, int[] fieldList) throws SQLException
    {
        ResultSet rs =  null;
        java.util.ArrayList v =  null;
        try
        {
            rs =  ps.executeQuery();
            v = new java.util.ArrayList();
            while(rs.next())
            {
                if(fieldList == null)
                    v.add(decodeRow(rs));
                else 
                    v.add(decodeRow(rs, fieldList));
            }
            return (TaxsequenceBean[])v.toArray(new TaxsequenceBean[0]);
        }
        finally
        {
            if (v != null) { v.clear(); v = null;}
            getManager().close(rs);
        }
    }

    ///////////////////////////////////////////////////////////////////////
    // LISTENER 
    ///////////////////////////////////////////////////////////////////////
    private TaxsequenceListener listener = null;

    /**
     * Registers a unique TaxsequenceListener listener.
     */
    //66.5
    public void registerListener(TaxsequenceListener listener) {
        this.listener = listener;
    }

    /**
     * Before the save of the TaxsequenceBean pObject.
     *
     * @param pObject the TaxsequenceBean pObject to be saved
     */
    //67
    void beforeInsert(TaxsequenceBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeInsert(pObject);
    }

    /**
     * After the save of the TaxsequenceBean pObject.
     *
     * @param pObject the TaxsequenceBean pObject to be saved
     */
    //68
    void afterInsert(TaxsequenceBean pObject) throws SQLException {
        if (listener != null)
            listener.afterInsert(pObject);
    }

    /**
     * Before the update of the TaxsequenceBean pObject.
     *
     * @param pObject the TaxsequenceBean pObject to be updated
     */
    //69
    void beforeUpdate(TaxsequenceBean pObject) throws SQLException {
        if (listener != null)
            listener.beforeUpdate(pObject);
    }

    /**
     * After the update of the TaxsequenceBean pObject.
     *
     * @param pObject the TaxsequenceBean pObject to be updated
     */
    //70
    void afterUpdate(TaxsequenceBean pObject) throws SQLException {
        if (listener != null)
            listener.afterUpdate(pObject);
    }

    ///////////////////////////////////////////////////////////////////////
    // UTILS  
    ///////////////////////////////////////////////////////////////////////

    /**
     * Retrieves the manager object used to get connections.
     *
     * @return the manager used
     */
    //2
    Manager getManager() {
        return Manager.getInstance();
    }

    /**
     * Frees the connection.
     *
     * @param c the connection to release
     */
    void freeConnection(Connection c) {
        getManager().releaseConnection(c); // back to pool
    }
    /**
     * Gets the connection.
     */
    Connection getConnection() throws SQLException {
        return getManager().getConnection();
    }

// class+ 

// class- 

}
